/*
Problem 11: Check if One String is Rotation of Another

Description:
Given two strings `s` and `goal`, return true if `goal` can be obtained
by rotating `s`, otherwise return false.

Example 1:
Input: s = "abcde", goal = "cdeab"
Output: true

Example 2:
Input: s = "abcde", goal = "abced"
Output: false
*/

function isRotation(s, goal) {
  // Write your solution here
}
